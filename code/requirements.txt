# Exact versions used to produce the paper's results (macOS/Apple-Silicon, Python 3.9).
# torch uses the MPS backend when available; everything falls back to CPU.
numpy==2.0.2
pandas==2.3.3
scipy==1.13.1
scikit-learn==1.6.1
statsmodels==0.14.6
matplotlib==3.9.4
torch==2.8.0
